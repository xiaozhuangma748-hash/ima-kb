# IMA 知识库 Web 后台 · PRD

> **版本**:v1.1
> **日期**:2026-07-09
> **作者**:知识库团队
> **状态**:已实现（技术方案从 Streamlit 调整为 FastAPI）

---

## 1. 产品概述

### 1.1 产品定位

基于现有 IMA 个人知识库(殡葬政策 RAG 系统)的 **Web 后台**,面向 1-5 人内网用户,提供浏览器端的知识库管理、AI 问答、文档入库、搜索、数据分析等功能,无需安装客户端。

### 1.2 核心价值

| 价值点 | 说明 |
|---|---|
| **零安装** | 同事浏览器打开即用,无需装 Python/依赖 |
| **全功能** | 覆盖 CLI/REPL 的 17 类功能,Web 化交互 |
| **内网安全** | 本地部署,数据不出内网,无云依赖 |
| **智能化** | 宠物管理员 + 混合检索 + 人格风格,继承 CLI 全部 AI 能力 |

### 1.3 范围边界

**本期范围(v1.0)**:
- 7 个 Web 页面(问答/入库/搜索/分析/仪表盘/图谱/宠物)
- 单用户模式(内网开放访问,共享一个知识库)
- FastAPI + 单页 HTML+JS 技术栈(复用现有 core/ 模块)

**不在本期范围**:
- 多用户账号系统(留待 v2.0)
- 移动端适配(桌面优先)
- 公网部署/HTTPS
- 付费/订阅

---

## 2. 用户画像

### 2.1 目标用户

| 角色 | 占比 | 核心诉求 | 典型场景 |
|---|---|---|---|
| **政策研究员** | 60% | 快速查政策、AI 问答 | 浏览器提问"退役军人抚恤金标准",获取带引用的回答 |
| **资料管理员** | 20% | 批量入库、整理文档 | 拖拽 10 个 PDF 上传,自动解析+标签+图谱 |
| **数据分析员** | 15% | Excel 分析、报告生成 | 上传殡葬服务收费表,自动统计+AI 解读 |
| **管理员** | 5% | 系统维护、质量检查 | 查看仪表盘,跑同步/去重 |

### 2.2 用户特征

- 不懂技术,不会用命令行
- 浏览器是主要工作环境
- 期望"傻瓜式"操作:上传→等待→看结果
- 重视响应速度:超过 3 秒无反馈会焦虑

---

## 3. 功能需求

### 3.1 功能架构

```
IMA Web 后台
├── 💬 AI 问答(核心)
│   ├── 对话式 RAG 问答
│   ├── 人格风格切换(4 种)
│   ├── 流式输出 + 引用编号
│   └── 多轮对话历史
├── 📥 文档入库(核心)
│   ├── 拖拽上传(多文件)
│   ├── URL 入库(网页抓取)
│   ├── 剪贴板入库(文本/截图)
│   ├── 自动解析 + 标签 + 图谱
│   └── 入库进度实时显示
├── 🔍 搜索(核心)
│   ├── 混合检索(BM25 + 向量 + RRF)
│   ├── LLM 重排序开关
│   ├── 标签多选筛选
│   └── 结果高亮 + 相关度
├── 📊 数据分析(核心)
│   ├── Excel 上传(多 sheet)
│   ├── 自动统计 + 字符图
│   ├── AI 解读趋势
│   └── 导出报告
├── 📈 仪表盘
│   ├── 知识库总览(文档数/标签/图谱)
│   ├── 质量告警
│   ├── 最近入库列表
│   └── 存储用量
├── 🕸️ 知识图谱
│   ├── vis.js 交互可视化
│   ├── 邻居查询
│   └── 导出 HTML
└── 🐾 宠物管理(可选)
    ├── 领养/改名
    ├── 喂食/玩耍/训练
    └── 风格切换
```

### 3.2 详细需求

#### 3.2.1 AI 问答页(P0)

| 需求 | 优先级 | 验收标准 |
|---|---|---|
| 对话输入框 | P0 | 支持多行文本,Ctrl+Enter 发送 |
| 流式输出 | P0 | 首字 < 2s,逐字显示,带 ⏺ 标记 |
| 引用编号 | P0 | 回答末尾显示 [1][2],点击跳转来源 |
| 来源卡片 | P0 | 显示文档标题 + 命中片段 + 相关度 |
| 人格切换 | P1 | 顶部 4 个标签(scholar/warrior/artisan/auto) |
| 多轮历史 | P0 | 保留当前会话全部历史,可清空 |
| 清空对话 | P0 | 二次确认后清空 |
| 输入示例 | P2 | 空状态显示 3 个示例问题可点击 |

#### 3.2.2 文档入库页(P0)

| 需求 | 优先级 | 验收标准 |
|---|---|---|
| 拖拽上传区 | P0 | 支持多文件,显示文件名+大小+格式图标 |
| 格式支持 | P0 | PDF/Word/Excel/PPT/MD/TXT/HTML/图片(11 种) |
| URL 入库 | P1 | 输入 URL,trafilatura 抓取正文 |
| 剪贴板入库 | P2 | 粘贴文本/截图,自动 OCR |
| 上传进度 | P0 | 每个文件独立进度条:解析→分块→标签→入库 |
| 自动标签 | P0 | 入库后显示 LLM 生成的 3-5 个标签 |
| 失败重试 | P1 | 单文件失败可重试,不影响其他文件 |
| 入库结果 | P0 | 显示:文档标题/标签/分块数/状态 |

#### 3.2.3 搜索页(P0)

| 需求 | 优先级 | 验收标准 |
|---|---|---|
| 搜索框 | P0 | 支持中文分词,Enter 触发 |
| 检索模式 | P1 | 开关:启用向量检索 / 启用 LLM 重排序 |
| 标签筛选 | P0 | 多选下拉,选中的标签 AND 关系 |
| 结果列表 | P0 | 文档标题 + 命中片段(高亮) + 标签 + 相关度分数 |
| 排序 | P1 | 按相关度/按时间/按文档名 |
| 分页 | P2 | 每页 10 条,加载更多 |
| 空状态 | P0 | 无结果时提示"未找到,试试其他关键词" |

#### 3.2.4 数据分析页(P0)

| 需求 | 优先级 | 验收标准 |
|---|---|---|
| Excel 上传 | P0 | 支持 .xlsx/.xls,多 sheet |
| Sheet 切换 | P0 | 顶部标签页切换 |
| 自动统计 | P0 | 每列:类型/空值数/唯一值/最小/最大/均值 |
| 字符图 | P0 | 数值列显示 ASCII 柱状图 |
| 数据预览 | P0 | 前 10 行表格 |
| AI 解读 | P1 | 按钮"AI 分析",LLM 生成趋势解读 |
| 导出 | P2 | 导出 Markdown 报告 |

#### 3.2.5 仪表盘(P1)

| 需求 | 优先级 | 验收标准 |
|---|---|---|
| 总览卡片 | P0 | 文档数/分块数/标签数/图谱节点数 |
| 标签分布 | P1 | 柱状图 Top 10 |
| 最近入库 | P1 | 列表显示最近 5 个文档 |
| 质量告警 | P1 | 空文档/超长块/低质量内容警告 |
| 存储用量 | P2 | uploads/cache/chroma/models 占用 |

#### 3.2.6 知识图谱(P1)

| 需求 | 优先级 | 验收标准 |
|---|---|---|
| 可视化 | P0 | vis.js 交互,节点可拖拽 |
| 节点颜色 | P0 | document=橙/region=青/agency=番茄/topic=紫 |
| 邻居查询 | P0 | 点击节点显示邻居列表 |
| 导出 HTML | P1 | 下载自包含 HTML 文件 |
| 重建图谱 | P2 | 按钮"重新构建"(调 LLM) |

#### 3.2.7 宠物管理(P2,可选)

| 需求 | 优先级 | 验收标准 |
|---|---|---|
| 宠物状态 | P2 | 显示等级/经验/心情/饱食/能量 |
| 喂食/玩耍/训练 | P2 | 3 个按钮,点击后状态变化 |
| 风格切换 | P2 | 4 种人格风格切换 |
| 像素艺术 | P2 | ASCII 艺术转图片显示 |

---

## 4. 页面设计

### 4.1 信息架构

```
顶部导航栏
├── Logo + 标题"IMA 知识库"
├── 当前页面名称
└── 全局搜索框(快捷键 /)

左侧导航栏(可折叠)
├── 💬 AI 问答
├── 📥 文档入库
├── 🔍 搜索
├── 📊 数据分析
├── 📈 仪表盘
├── 🕸️ 知识图谱
└── 🐾 宠物管理

主内容区
└── 各页面内容
```

### 4.2 页面布局规范

- **布局**:宽屏布局,左侧导航栏 + 右侧主内容区
- **主题**:亮色简约风（白色背景 + 橙色主色 #F59E0B + 青色强调 #06B6D4 + 卡片阴影 + 顶部色条）
- **配色**:背景 #fff / 主色 #F59E0B(橙) / 强调 #06B6D4(青) / 卡片 #fff + 阴影
- **字体**:系统默认,等宽字体用于代码/数据
- **间距**:页面边距 2rem,卡片间距 1rem

### 4.3 交互规范

| 场景 | 反馈 |
|---|---|
| 加载中 | Spinner + "正在..."文案 |
| 成功 | 绿色 Toast 提示 |
| 失败 | 红色 Toast + 错误详情 |
| 长任务 | 进度条 + 当前步骤 |
| 空状态 | 插图 + 引导文案 + CTA 按钮 |

---

## 5. 技术方案

### 5.1 技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 前端框架 | 单页 HTML + JS | `web/templates/index.html` + `web/static/app.js`，侧边栏切换 7 页面 |
| 图表 | 原生 JS + vis.js(CDN) | 搜索结果/数据分析/知识图谱 |
| 后端 | FastAPI + uvicorn | 7 个路由模块，复用 core/ |
| 通信 | SSE (Server-Sent Events) | AI 问答流式输出 |
| 部署 | `ima web` 或 `uvicorn web.app:create_app --factory` | 内网访问 |

### 5.2 模块复用

| Web 页面 | 复用的 core 模块 | 后端 API 路由 |
|---|---|---|
| AI 问答 | `core/pet/administrator.py` + `core/persona/prompts.py` | `web/routes/qa.py`（SSE 流式） |
| 文档入库 | `core/ingestion/parser.py` + `core/classify/tagger.py` | `web/routes/ingest.py` |
| 搜索 | `core/retrieval/hybrid.py` + `core/retrieval/rerank.py` | `web/routes/search.py` |
| 数据分析 | `core/analyze/analyzer.py` | `web/routes/analyze.py` |
| 仪表盘 | `core/storage.py` + `core/sync/checker.py` | `web/routes/stats.py` |
| 知识图谱 | `core/graph/store.py` + `core/graph/visualizer.py` | `web/routes/graph.py` |
| 宠物管理 | `core/pet/pet.py` + `core/pet/interact.py` | `web/routes/pet.py` |

### 5.3 部署架构

```
内网用户浏览器
    ↓ http://192.168.x.x:8501
部署机器(Mac/Linux)
    ├── FastAPI 进程(ima web / uvicorn，默认端口 8501)
    ├── .venv(Python 环境)
    ├── storage/(知识库数据)
    └── .env(API Key)
```

---

## 6. 优先级与里程碑

### 6.1 优先级

| 优先级 | 页面/功能 | 说明 |
|---|---|---|
| **P0** | AI 问答 + 文档入库 + 搜索 + 数据分析 | 核心价值,必须做 |
| **P1** | 仪表盘 + 知识图谱 | 增强体验,应做 |
| **P2** | 宠物管理 | 锦上添花,可做 |

### 6.2 里程碑

| 里程碑 | 内容 | 交付物 |
|---|---|---|
| **M1: 核心功能** | AI 问答 + 文档入库 + 搜索 | 3 个页面可用 |
| **M2: 数据分析** | 数据分析页 | Excel 上传 + 统计 + AI 解读 |
| **M3: 仪表盘** | 仪表盘增强 | 总览 + 告警 + 最近入库 |
| **M4: 图谱 + 宠物** | 知识图谱 + 宠物管理 | 完整 7 页面 |

---

## 7. 验收标准

### 7.1 功能验收

- [ ] AI 问答:流式输出,带引用,人格切换可用
- [ ] 文档入库:拖拽 10 个 PDF,全部成功,显示标签
- [ ] 搜索:关键词 + 标签筛选,结果高亮
- [ ] 数据分析:Excel 多 sheet,统计 + 字符图 + AI 解读
- [ ] 仪表盘:总览数据准确,告警正确
- [ ] 知识图谱:节点可交互,导出可用
- [ ] 内网访问:同事浏览器可打开,响应 < 3s
- [ ] ✅ 已实现:FastAPI 后端 + 单页 HTML 前端,7 页面全部可用

### 7.2 非功能验收

- [ ] 性能:首屏 < 2s,AI 首 token < 3s
- [ ] 兼容:Chrome/Edge/Safari 最新版
- [ ] 稳定:连续运行 8 小时无崩溃
- [ ] 安全:.env 不暴露,API Key 不泄露

---

## 8. 风险与对策

| 风险 | 概率 | 影响 | 对策 |
|---|---|---|---|
| FastAPI 并发限制 | 低 | 1-5 人内网使用足够 | 单进程 uvicorn 即可，需要时加 workers |
| 大文件上传超时 | 中 | 上传 > 50MB PDF 失败 | 分块上传 + 进度条 |
| LLM API 限流 | 低 | 高频问答被限流 | 加重试 + 友好降级提示 |
| 向量模型加载慢 | 中 | 首次启动 > 30s | 预加载 + 缓存 |

---

## 9. 后续规划(v2.0)

- 多用户账号系统(FastAPI + JWT)
- 移动端适配(响应式布局)
- 公网部署 + HTTPS
- 协作功能(共享文档/评论)
- API 开放(供第三方集成)

---

**评审签字**:

| 角色 | 签字 | 日期 |
|---|---|---|
| 产品 | ____ | ____ |
| 开发 | ____ | ____ |
| 设计 | ____ | ____ |
